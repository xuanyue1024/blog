# 项目开发注意事项

本文件列出项目日常开发中需要注意的要点，侧重代码质量、可维护性、可访问性与性能。

## 快速提示
- 常用本地命令：`pnpm install`、`pnpm format`、`pnpm lint`、`pnpm typecheck`、`pnpm build`。

## Astro 与架构
- 遵循 Astro 最佳实践：页面保持薄（仅负责路由 + 数据组合），组件小且单一职责，尽量在服务端完成数据获取，减少不必要的客户端渲染。
- 组件放置：`src/components/`；控制类放 `control/`；widget 放 `widget/`；页面放 `src/pages/`；内容放 `src/content/`。

## TypeScript 与类型
- 开启严格模式，尽量避免 `any`；必要时添加注释说明原因。
- 优先显式类型、使用 `readonly` 修饰可变资料，函数返回值显式标注。
- 导出类型/接口建议使用具名导出，便于维护与重构。

## 代码风格与格式化
- 遵循项目中 `biome.json` 的配置作为基线，提交前运行格式化与 lint。
- 保持一致的缩进与命名风格（参照仓库现有约定）。

## 注释规范（必须）
- 所有非平凡函数、组件或复杂逻辑必须有中文注释，说明“做什么”与“为什么”。
- 注释至少包含功能概述、主要输入/输出、边界条件与副作用。复杂逻辑应分段说明实现思路与权衡。

## 命名与导出
- 文件/路由命名建议小写短横线（kebab-case）或遵循仓库既有风格。
- 优先使用具名导出（named export）；仅在确有必要时使用默认导出（default export）。

## 国际化（i18n）
- 不要在组件中硬编码可见文本，使用 `src/i18n/` 的键与翻译文件。
- 新增用户可见文本时，应在翻译文件中补全或标注待翻译。

## 可访问性（a11y）
- 使用语义化 HTML（`button`、`nav`、`main` 等）；表单控件需关联 `label`。
- 图片必须提供 `alt`；交互控件需支持键盘操作与可聚焦状态。

## 性能
- 图片使用合适尺寸与现代格式，按需启用懒加载（`loading="lazy"`）与低质量占位（LQIP）。
- 避免在客户端 bundle 中引入大型库；使用按需/动态导入（code-splitting）。
- 对频繁运行的逻辑关注算法复杂度并考虑缓存策略。

## 安全与隐私
- 任何密钥/凭证不得写入源码或 `public` 目录；使用环境变量并确保私密文件被 `.gitignore` 忽略。
- 对用户输入做必要校验与转义，避免注入类风险。

## 测试建议
- 关键业务逻辑建议补充单元测试（推荐 Vitest + Testing Library for Svelte）；复杂交互建议 E2E（Playwright）。
- 测试侧重行为而非实现细节，保持测试可读性与稳定性。

## 文档与内容约定
- Markdown 内容应包含完整 frontmatter（title、date、tags、lang 等）；发布前检查元数据与 slug。
- 界面/用户可见变更应补充简短中文说明与验证步骤。

## 可重用性与封装
- 避免重复实现，抽取共用逻辑到 `src/utils/` 或公共组件并明确契约（输入/输出）。
- 组件应明确定义 props/事件，尽量避免隐式副作用。

## 日志与调试
- 开发时可使用日志与调试信息，生产环境避免输出大量敏感或冗余日志。
- 使用可配置的日志级别，生产环境关闭调试输出。

## 依赖管理
- 使用 `pnpm` 并维护 lockfile；更新依赖前阅读变更日志与 breaking changes。
- 避免引入不维护或弃用的包，优先社区活跃的方案。

## 本地验证清单
- 通过格式化/静态检查/类型检查：`pnpm format`、`pnpm lint`、`pnpm typecheck`。
- 本地浏览器手动验证：关键路由、表单流程、重要交互、移动端显示、meta 信息（title/description/og）。
- 对于文档、说明、配置注释等低风险改动，如果你能合理判断不会影响运行结果或页面行为，可以不强制运行测试或构建；优先把验证成本留给真正影响代码路径、样式渲染或交互逻辑的改动。

## 常见问题与快速修复
- 端口冲突：检查占用端口并修改启动端口或停止占用进程。
- 环境变量问题：确认 `.env` 文件加载与服务重启。
- 依赖问题：尝试删除 `node_modules` 与 lockfile，重新运行 `pnpm install`。

